#!/usr/bin/env bash

declare -x QUANTUMVIM_DIR="${QUANTUMVIM_DIR:-QVIM_DIR_VAR}"
declare -x QUANTUMVIM_CACHE_DIR="${QUANTUMVIM_CACHE_DIR:-CACHE_DIR_VAR}"

exec -a qvim nvim -u "$QUANTUMVIM_DIR/init.lua" "$@"