local t = {
    ["<"] = { "<gv", 'Indent right' },
}

local opts = {
    mode = true,
    noremap = true,
    nowait = true,
    silent = true,
    script = true,
    expr = true,
    unique = true,
    buffer = true,
    callback = true,
    desc = "",
}
local wk = {
    prefix = "",
    group = "",
}
