---The QV_STRING_EXT_PLUGIN_NAME_VAR configuration file of the QV_STRING_PLUGIN_NAME_VAR plugin
local M = {}

local Log = require "qvim.integrations.log"

---Registers the global configuration scope for QV_STRING_PLUGIN_NAME_VAR
M.config = function()
  qvim.integrations.QV_PLUGIN_NAME_VAR.QV_EXT_PLUGIN_VAR = {
    active = true,
    on_config_done = nil,
    keymaps = { },
    options = {
        -- QV_EXT_PLUGIN_VAR option configuration

    },
  }
end

---The QV_STRING_EXT_PLUGIN_NAME_VAR setup function. The module will be required by 
---this function and it will call the respective setup function.
---A on_config_done function will be called if the plugin implements it.
M.setup = function()
  local status_ok, QV_EXT_PLUGIN_VAR = pcall(reload, "QV_STRING_EXT_PLUGIN_NAME_VAR")
  if not status_ok then
    Log:warn(string.format("The extension '%s' could not be loaded.", QV_EXT_PLUGIN_VAR))
    return
  end

  local _QV_PLUGIN_NAME_VAR_QV_EXT_PLUGIN_VAR = qvim.integrations.QV_PLUGIN_NAME_VAR.QV_EXT_PLUGIN_VAR
  QV_EXT_PLUGIN_VAR.setup(_QV_PLUGIN_NAME_VAR_QV_EXT_PLUGIN_VAR.options)

  if _QV_PLUGIN_NAME_VAR_QV_EXT_PLUGIN_VAR.on_config_done then
    _QV_PLUGIN_NAME_VAR_QV_EXT_PLUGIN_VAR.on_config_done()
  end
end

return M